name: Deploy Courseware

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Demo1
        run: |
          cd demo1-bar-model
          npm install
          npm run build
          mkdir -p ../publish/demo1-bar-model
          cp -R dist/* ../publish/demo1-bar-model/
      - uses: actions/upload-pages-artifact@v3
        with:
          path: publish

  deploy:
    environment:
      name: github-pages
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/deploy-pages@v4
